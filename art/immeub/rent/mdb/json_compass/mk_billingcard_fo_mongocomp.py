"""
art/immeub/rent/mdb/json_compass/mk_billingcard_fo_mongocomp.py
  Makes Billing Card MongoDB collection 'docs'
    for copying-and-pasting into Compass.

  This is not a 'production' function,
    rather, consider this as a provisory 'facilitator' to
    adding test data into MongoDB.

  Because the output here can be copied and pasted as a
    collection doc and seen then served through an API endpoint.

  Because the first endpoints will be 'GET'.
  Then 'POST' and 'PUT' may come up for allowing inputing data,
    giving form to the app
    (data input must then be done via a web-dashboard).
"""


def output_billingcard_json_doc():
  pass

